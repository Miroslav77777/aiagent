# Данные для подключения к базе данных PostgreSQL (внутри Docker)
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin_password
POSTGRES_DB=papers_db

# Ссылка на локальный Ollama
# host.docker.internal обеспечивает доступ из контейнера к localhost машины
OLLAMA_BASE_URL=http://host.docker.internal:11434
